;;; sej-template.el --- template file -*- no-byte-compile: t; lexical-binding: t; -*-


;;; Commentary:
;; Provides interactive commands to switch between GitHub accounts
;; using the gh CLI, useful when pushing to different remotes in Magit.

;;; Code:


;; Local variables:
;; byte-compile-warnings: (not obsolete free-vars)
;; End:

(provide 'sej-template)
;;; sej-template.el ends here
