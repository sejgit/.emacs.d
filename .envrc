# direnv configuration for .emacs.d development
# This keeps your development environment isolated

# Add local bin to PATH if needed
# export PATH="$PWD/bin:$PATH"

# Set EMACS to use specific version if desired
# export EMACS=/opt/homebrew/bin/emacs

# Enable pre-commit hooks
# Run: pre-commit install

echo "✓ .emacs.d development environment loaded"
